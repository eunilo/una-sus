# Dependências principais para o scraper UNA-SUS
requests>=2.31.0          # Para requisições HTTP
pandas>=2.0.0             # Para manipulação de dados CSV
beautifulsoup4>=4.12.0    # Para parsing HTML
lxml>=4.9.0               # Parser XML/HTML (mais rápido que html.parser)

# Dependências opcionais para desenvolvimento
# flake8>=6.0.0           # Para linting (descomente se necessário)
# black>=23.0.0           # Para formatação de código (descomente se necessário) 